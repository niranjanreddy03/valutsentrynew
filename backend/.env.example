# Vault Sentry Backend Configuration
# Copy this file to .env and fill in the values

# Application
DEBUG=True
ENVIRONMENT=development

# Supabase Configuration (required for storing scan results)
# Get these from: Supabase Dashboard → Settings → API 
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-anon-key
# Service role key for backend operations (bypasses RLS)
# Get this from: Supabase Dashboard → Settings → API → service_role (secret)
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Redis (for Celery task queue)
REDIS_URL=redis://localhost:6379/0

# Security
SECRET_KEY=your-secret-key-here

# CORS - Add all origins that need to access the API
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:3000,http://127.0.0.1:8000

# GitHub Integration (for private repository scanning)
# Create a GitHub Personal Access Token with repo access:
# https://github.com/settings/tokens/new?scopes=repo
GITHUB_TOKEN=your-github-personal-access-token

# GitHub OAuth App (for GitHub login)
# Create at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret

# GitHub Webhook Secret (for automated scanning on push)
GITHUB_WEBHOOK_SECRET=your-webhook-secret
